version: '3.8'

services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: redis-stack
    ports:
      - "6378:6379"
    volumes:
      - /home/mobcoder/Desktop/redis-volume:/data
    environment:
      - REDIS_ARGS=--requirepass 123456 --appendonly yes
